package com.epita.fitnesstracker;

import android.os.Bundle;

import androidx.appcompat.app.AppCompatActivity;

/**
 * TODO: Display the full workout detail — exercises with sets, reps, and weight.
 *
 * Students should:
 * 1. Receive the workout ID via Intent extras.
 * 2. Call GET /workouts/:id to fetch the workout with its exercises.
 * 3. Display the workout info and a list of exercises in a RecyclerView.
 */
public class WorkoutDetailActivity extends AppCompatActivity {

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_workout_detail);

        // TODO: Implement workout detail screen
    }
}
